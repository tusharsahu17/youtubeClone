import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PaidExams = ({item}) => {
  return (
    <View style={styles.container}>
      <Text>PaidExams</Text>
    </View>
  );
};

export default PaidExams;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
